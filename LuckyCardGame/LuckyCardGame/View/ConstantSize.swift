import UIKit

enum ConstantSize {
    static let subViewWidth = totalWidth - 2*padding
    static let totalHeight = UIScreen.main.bounds.height
    static let totalWidth = UIScreen.main.bounds.width
    static let padding = totalWidth / 30
    static let cornerRadiusDegree: CGFloat = 10
    static let yellowViewHeight: CGFloat = 44
}
